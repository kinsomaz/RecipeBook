<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Chats">

  <androidx.constraintlayout.widget.ConstraintLayout
      android:id="@+id/chatFirstLayout"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      app:layout_constraintTop_toTopOf="parent">

  <!-- back -->
  <View
      android:id="@+id/back_chatsIcon"
      android:layout_width="45.68dp"
      android:layout_height="45.68dp"
      android:layout_marginStart="20dp"
      android:layout_marginTop="74.23dp"
      android:background="@drawable/back1"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent"/>

  <!-- Chats -->
  <TextView
      android:id="@+id/chatsPage"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginStart="17.13dp"
      android:text="@string/chatsPage"
      android:textAppearance="@style/chatsPage"
      android:lineSpacingExtra="13sp"
      android:gravity="center_vertical"
      app:layout_constraintStart_toEndOf="@+id/back_chatsIcon"
      app:layout_constraintTop_toTopOf="@+id/back_chatsIcon"
      app:layout_constraintBottom_toBottomOf="@+id/back_chatsIcon" />

  <!-- logo -->
  <View
      android:id="@+id/logo_chat"
      android:layout_width="38.83dp"
      android:layout_height="42.25dp"
      android:layout_marginEnd="25dp"
      android:background="@drawable/logo"
      android:elevation="20dp"
      android:outlineAmbientShadowColor="#14000000"
      android:outlineSpotShadowColor="#80000000"
      app:layout_constraintBottom_toBottomOf="@+id/back_chatsIcon"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintTop_toTopOf="@+id/back_chatsIcon" />

  </androidx.constraintlayout.widget.ConstraintLayout>

  <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/chatRecyclerView"
      android:layout_width="match_parent"
      android:layout_height="0dp"
      android:layout_marginTop="30dp"
      app:layout_constraintTop_toBottomOf="@+id/chatFirstLayout"
      />

</androidx.constraintlayout.widget.ConstraintLayout>